@using Handbook.Shared
@using Handbook.Client.Services.UserService

<h3>@Header</h3>

<EditForm Model="User" OnSubmit="OnSubmit">
    <div>
        <div class="row">
            <label>Имя</label>
            <InputText @bind-Value="@User.FirstName" class="form-control"></InputText>
        </div>
        <div class="row">
            <label>Отчество</label>
            <InputText @bind-Value="@User.Patronymic" class="form-control"></InputText>
        </div>
        <div class="row">
            <label>Фамилия</label>
            <InputText @bind-Value="@User.LastName" class="form-control"></InputText>
        </div>
    </div>
    <button type="submit" class="btn btn-@ButtonStyle">@ButtonText</button>
</EditForm>

@code {
    [Parameter]
    public string? Header { get; set; } 

    [Parameter]
    public User? User { get; set; }

    [Parameter]
    public string? ButtonText { get; set; } 

    [Parameter]
    public string ButtonStyle { get; set; } = "info";

    [Parameter]
    public EventCallback OnSubmit { get; set; }
}
